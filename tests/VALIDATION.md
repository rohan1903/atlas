Manual commands (replace placeholders):

```powershell
atlas scan <path-to-repo> --force
atlas architecture <path-to-repo>
atlas top-files <path-to-repo> --limit 20
atlas flow <feature> <path-to-repo>
atlas learn <topic> <path-to-repo>
atlas explain <topic> <path-to-repo>
```

**Automated reports:** see [benchmarks/README.md](benchmarks/README.md) (`run-reports.ps1` runs these and writes markdown per repo).
